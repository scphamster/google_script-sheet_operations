import Integer = GoogleAppsScript.Integer;

export function getValueFromCell(row: Integer, column: Integer): string {
    return "implement me";
}

function findColumnWithHeader(header: string) {
    // find column with header in spreadsheet
}

